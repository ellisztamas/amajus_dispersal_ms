name: amajus_mating_ms
channels:
  - defaults
dependencies:
  - pip
  - numpy==1.26
  - scipy
  - pandas
  - bottleneck>=1.3.6
  - python=3.12
  - tqdm
  - ipykernel
  - pip:
    - faps
    - 02_library/python
    - wquantiles
